TODO:

- Add notification / modal type thing
- Add a hook to delete all entries when a user is deleted
- Use this loader for loading events
- limit quantity when adding a meal to 5? OTherwise it does x inserts, clearing cache etc
- check user's can only edit entries and data belonging to those entries that are theirs
- hook into wp_Send_json and log an errors?
- replace "Remove" with foo icon
- add a notificion when meal added ok
- add validation to add meal to entry (i.e. ensure they have selected a meal!)